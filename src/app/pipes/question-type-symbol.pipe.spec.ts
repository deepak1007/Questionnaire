import { QuestionTypeSymbolPipe } from './question-type-symbol.pipe';

describe('QuestionTypeSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionTypeSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
